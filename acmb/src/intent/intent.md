### thi part that actually makes Academeet feel alive.
Intent → Match Detection → Notification Pipeline cleanly

### When a user creates an intent:

👉 System should:
Find similar users (same intent / skill)
Emit notification events
Let your existing pipeline handle the rest

